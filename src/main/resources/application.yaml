spring:
  application:
    name: url-shortener
  profiles:
    active: dev
server:
  servlet:
    context-path: /api/v1/shorten
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

